# PostgreSQL connection string (used by Prisma)
DATABASE_URL="postgresql://user:password@localhost:5432/briefly"

# Claude API key, used for summarization, Bengali rewriting, and categorization
ANTHROPIC_API_KEY=""

# Public site URL, used for metadata, sitemap, and RSS feed generation
NEXT_PUBLIC_SITE_URL="http://localhost:3000"

# Shared secret required by the /api/cron/fetch-news route
CRON_SECRET=""
